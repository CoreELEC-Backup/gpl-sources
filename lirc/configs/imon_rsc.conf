# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             imon_rsc
    label:          Soundgraph  iMON RSC
    menu:           usb
    lircd_conf:     imon/imon-rsc.lircd.conf
    Note:           Usually only lirccode devices are supported bu this
                    capture device
